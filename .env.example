# Smart Pet MQTT — broker connection for the TypeScript client
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
# Client id prefix; a random suffix is appended
MQTT_CLIENT_ID=smart-pet

# Tenant this process acts for (breeding kennel slug for B2B, household id for B2C)
KENNEL_ID=home
